{
  "name": "syngrafo-frontend",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/react-client",
    "packages/editor",
    "packages/shared",
    "packages/audio",
    "packages/video"
  ],
  "scripts": {
    "build": "cd packages/react-client && bun run build.ts --minify",
    "dev": "cd packages/react-client && bun run dev",
    "install-all": "bun install"
  }
}
